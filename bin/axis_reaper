#!/usr/bin/env python
"""Monitor the Axis collection by looking for books that have been removed."""


from palace.manager.api.axis import AxisCollectionReaper
from palace.manager.core.scripts import RunCollectionMonitorScript

RunCollectionMonitorScript(AxisCollectionReaper).run()
