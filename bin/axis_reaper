#!/usr/bin/env python
"""Monitor the Axis collection by looking for books that have been removed."""


from palace.api.axis import AxisCollectionReaper
from palace.core.scripts import RunCollectionMonitorScript

RunCollectionMonitorScript(AxisCollectionReaper).run()
