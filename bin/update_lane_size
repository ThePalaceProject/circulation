#!/usr/bin/env python
"""
Manually kick off the lane size sweep Celery task.

Enqueues update_lane_sizes_sweep, which fans out size updates for every lane
and fires finalize_lane_size_update once all updates are complete.
"""
from palace.manager.scripts.lane import UpdateLaneSizesSweepScript

UpdateLaneSizesSweepScript().run()
