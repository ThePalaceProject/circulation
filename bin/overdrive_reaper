#!/usr/bin/env python
"""Monitor the Overdrive collections by looking for books with lost licenses."""


from palace.api.overdrive import OverdriveCollectionReaper
from palace.core.scripts import RunCollectionMonitorScript

RunCollectionMonitorScript(OverdriveCollectionReaper).run()
