#!/usr/bin/env python
"""Push the configurations of one or more libraries to a library registry."""


from palace.api.registration.registry import LibraryRegistrationScript

LibraryRegistrationScript().run()
